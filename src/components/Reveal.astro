---
interface Props {
  delay?: number;
  as?: string;
  class?: string;
}
const { delay = 0, as: Tag = 'div', class: cls = '' } = Astro.props;
const style = delay ? `transition-delay: ${delay}ms;` : undefined;
---
<Tag class={`reveal ${cls}`} style={style}>
  <slot />
</Tag>
